/**
 * Implement a class named ClassRoom
 * @maxStudentsSize: (Number) and assigned to _maxStudentsSize
*/

export default class ClassRoom {
  /* ClassRoom */

  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
