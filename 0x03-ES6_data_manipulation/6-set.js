/**
 * setFromArray - function that returns a Set from an array.
 * @arr: array
 * return: Set
 */

const setFromArray = (arr) => {
  const uniqueEle = new Set(arr);
  return uniqueEle;
};

export default setFromArray;
